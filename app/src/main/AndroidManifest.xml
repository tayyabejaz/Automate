<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.innovidio.androidbootstrap">

    <application
        android:name=".BaseApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.ActivityPrivacyPolicy"></activity>
        <activity android:name=".activity.UserPreferencesActivity" />
        <activity
            android:name=".activity.UserProfileActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activity.FilterResultActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddNewCarActivity" />
        <activity
            android:name=".activity.FormActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".dashboard.SpeedDashboardActivity"
            android:launchMode="singleTask" /> <!-- <activity android:name=".Dialog.AddServiceDialog" /> -->
        <activity
            android:name=".dashboard.SetSpeedLimit"
            android:screenOrientation="portrait" />

        <service android:name=".service.FloatingViewService" />
    </application>
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="22" />

    <dist:module dist:instant="true" />

</manifest>